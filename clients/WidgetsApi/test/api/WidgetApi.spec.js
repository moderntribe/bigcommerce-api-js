/*
 * Widgets
 * Widgets API allows developers to programatically inject content into a BigCommerce theme. To learn how to use Widgets, see our [Widgets Overview](/api-docs/storefront/widgets/widgets-overview).
 *
 * OpenAPI spec version: 3.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.21
 *
 * Do not edit the class manually.
 *
 */
(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD.
    define(['expect.js', '../../src/index'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    factory(require('expect.js'), require('../../src/index'), require('../../src/BCApiClient'), require('../config'));
  } else {
    // Browser globals (root is window)
    factory(root.expect, root.Widgets);
  }
}(this, function(expect, Widgets, BCApiClient, config) {
  'use strict';

  var instance;

  beforeEach(function() {
    instance = new Widgets.WidgetApi(new BCApiClient(config));
  });

  describe('(package)', function() {
    describe('WidgetApi', function() {
      describe('createWidget', function() {
        it('should call createWidget successfully', async function() {
          // const createWidgetResponse = await instance.createWidget();
          // expect(createWidgetResponse.ok).to.be(true);
          // expect(createWidgetResponse.body.data).to.be.an('array');
        });
      });
      describe('deleteWidget', function() {
        it('should call deleteWidget successfully', async function() {
          // const deleteWidgetResponse = await instance.deleteWidget();
          // expect(deleteWidgetResponse.ok).to.be(true);
          // expect(deleteWidgetResponse.body.data).to.be.an('array');
        });
      });
      describe('getWidget', function() {
        it('should call getWidget successfully', async function() {
          // const getWidgetResponse = await instance.getWidget();
          // expect(getWidgetResponse.ok).to.be(true);
          // expect(getWidgetResponse.body.data).to.be.an('array');
        });
      });
      describe('getWidgets', function() {
        it('should call getWidgets successfully', async function() {
          const response = await instance.getWidgets();
          expect(response.ok).to.be(true);
          expect(response.body.data).to.be.an('array');
        });
      });
      describe('updateWidget', function() {
        it('should call updateWidget successfully', async function() {
          // const updateWidgetResponse = await instance.updateWidget();
          // expect(updateWidgetResponse.ok).to.be(true);
          // expect(updateWidgetResponse.body.data).to.be.an('array');
        });
      });
    });
  });

}));
