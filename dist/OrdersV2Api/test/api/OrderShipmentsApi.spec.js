'use strict';

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; }

/*
 * Orders V2
 * `/orders`endpoints integrate with point-of-sale, shipping, distribution, and inventory-management systems.  ## Order The Order object contains a record of the purchase agreement between a shopper and a merchant. To learn more about creating orders, see [Orders API Guide](/api-docs/orders/orders-api-overview).  ### Currency Fields  * `currency_code` - the display currency used to present prices to the shopper on the storefront. * `currency_exchange_rate`: the exchange rate between the store's default currency and the display currency; when the order is created by means of the V2 endpoints, this value is always 1 (only in the storefront this value can be different to 1).  The following additional fields are returned on orders when Multi-Currency is enabled on the store:  * `store_default_currency_code` - the store's default currency * `store_default_to_transactional_exchange_rate` - the exchange rate between the store's default currency and the transactional currency used in the order.  **Example:**  ```json {   //...   \"currency_id\": 4,   \"currency_code\": \"EUR\",   \"currency_exchange_rate\": 1,   \"default_currency_id\": 4,   \"default_currency_code\": \"EUR\",   \"store_default_currency_code: \"USD\",   \"store_default_to_transactional_exchange_rate\": \"100.0000000000\"   //... }  ```  ## Order Coupons Coupon code applied to an order.  ## Order Messages Messages associated with an order.  ## Order Products Product line items belonging to an order.  ## Order Shipping Addresses Customer shipping address belonging to an order.  ## Order Statuses Each order status represents a state in the order-fulfillment workflow.  ## Order Taxes Each tax applied to an order. This information can be useful for reporting purposes. All values are read-only.  ## Order Shipments Tracks a package consignment from an order that is shipped from the seller to the buyer.  ## Order Shipping Addresses Quotes Read Only. Gets all shipping quotes from an order.
 *
 * OpenAPI spec version: 2.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.21
 *
 * Do not edit the class manually.
 *
 */
(function (root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD.
    define(['expect.js', '../../src/index'], factory);
  } else if ((typeof module === 'undefined' ? 'undefined' : _typeof(module)) === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    factory(require('expect.js'), require('../../src/index'), require('../../src/BCApiClient'), require('../config'));
  } else {
    // Browser globals (root is window)
    factory(root.expect, root.OrdersV2);
  }
})(undefined, function (expect, OrdersV2, BCApiClient, config) {
  'use strict';

  var instance;

  beforeEach(function () {
    instance = new OrdersV2.OrderShipmentsApi(new BCApiClient(config));
  });

  describe('(package)', function () {
    describe('OrderShipmentsApi', function () {
      describe('createOrderShipments', function () {
        it('should call createOrderShipments successfully', _asyncToGenerator(function* () {
          // const createOrderShipmentsResponse = await instance.createOrderShipments();
          // expect(createOrderShipmentsResponse.ok).to.be(true);
          // expect(createOrderShipmentsResponse.body.data).to.be.an('array');
        }));
      });
      describe('deleteAllOrderShipments', function () {
        it('should call deleteAllOrderShipments successfully', _asyncToGenerator(function* () {
          // const deleteAllOrderShipmentsResponse = await instance.deleteAllOrderShipments();
          // expect(deleteAllOrderShipmentsResponse.ok).to.be(true);
          // expect(deleteAllOrderShipmentsResponse.body.data).to.be.an('array');
        }));
      });
      describe('deleteOrderShipment', function () {
        it('should call deleteOrderShipment successfully', _asyncToGenerator(function* () {
          // const deleteOrderShipmentResponse = await instance.deleteOrderShipment();
          // expect(deleteOrderShipmentResponse.ok).to.be(true);
          // expect(deleteOrderShipmentResponse.body.data).to.be.an('array');
        }));
      });
      describe('getAllOrderShipments', function () {
        it('should call getAllOrderShipments successfully', _asyncToGenerator(function* () {
          // const getAllOrderShipmentsResponse = await instance.getAllOrderShipments();
          // expect(getAllOrderShipmentsResponse.ok).to.be(true);
          // expect(getAllOrderShipmentsResponse.body.data).to.be.an('array');
        }));
      });
      describe('getCountShipments', function () {
        it('should call getCountShipments successfully', _asyncToGenerator(function* () {
          // const getCountShipmentsResponse = await instance.getCountShipments();
          // expect(getCountShipmentsResponse.ok).to.be(true);
          // expect(getCountShipmentsResponse.body.data).to.be.an('array');
        }));
      });
      describe('getOrderShipment', function () {
        it('should call getOrderShipment successfully', _asyncToGenerator(function* () {
          // const getOrderShipmentResponse = await instance.getOrderShipment();
          // expect(getOrderShipmentResponse.ok).to.be(true);
          // expect(getOrderShipmentResponse.body.data).to.be.an('array');
        }));
      });
      describe('updateOrderShipment', function () {
        it('should call updateOrderShipment successfully', _asyncToGenerator(function* () {
          // const updateOrderShipmentResponse = await instance.updateOrderShipment();
          // expect(updateOrderShipmentResponse.ok).to.be(true);
          // expect(updateOrderShipmentResponse.body.data).to.be.an('array');
        }));
      });
    });
  });
});